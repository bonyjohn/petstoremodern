/**
 * Order module: shopping cart checkout and order lifecycle.
 * Legacy mapping: replaces the Order Processing Center (OPC) order pipeline
 * (order EJBs and the associated workflow/state transitions) with a
 * simplified Spring-managed order service.
 */
package com.petstore.core.order;
